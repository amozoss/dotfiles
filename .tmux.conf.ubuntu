# Use as ~/.tmux.conf on Ubuntu (or any host) when you want this entrypoint.
# Shared behaviour lives in .tmux.conf (true color / TERM_GUI_COLORS / keys).
# Override path if your repo is not under ~/dotfiles.
source-file ~/dotfiles/.tmux.conf
